spring:
  application:
    name: customer
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/reviewlah-customer?useSSL=false
    username:
    password:
server:
  port: 8082
mybatis:
  mapper-locations: classpath:mapper/*.xml
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
